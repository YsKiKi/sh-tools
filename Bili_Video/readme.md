## 功能
将A+1站缓存的M4S文件对合并为MP4视频文件。

## 使用方法

### 1. 准备文件
- 下载安装 `ffmpeg` 并将其 `bin` 文件夹添加到 `Path`

### 2. 安装库
```bash
pip install -r requirements.txt
```

### 3. 运行脚本
```bash
python gui.py
```

## 注意事项
1. **FFmpeg必需**：确保已安装FFmpeg并加入系统环境变量